# run014 実験記録

## 1. 概要

- Run: `output/run014_20260822_viewport_gradient_baseline`
- 実行日時: 2026-08-22（ディレクトリ名による）
- 状態: 実行中
- 実験目的: viewport gradient baseline（ディレクトリ名による。詳細は要追記）
- 前run: run013と推定せず、要追記
- 比較対象: 要追記
- 注意: 本記録は学習を停止・lockせず、増加中のログを読み取った途中snapshotである。

## 2. 再現情報

- Git branch / commit / working tree / push状況: 実験成果物に記録がなく不明
- 環境: device `auto`、dtype `float32`、seed `0`。OS、GPU、CUDA、PyTorch、Pythonは不明。
- データ: `camera_poses_blender.json`、resolution scale 1.0、8枚ごとをtest、黒背景。

## 3. 学習条件

- 初期Gaussian 20,000、SH degree 3、初期opacity 0.1。
- 5,000 iterations、Adam、D-SSIM weight 0.2。
- LR: position 0.00016→0.0000016、SH DC/rest 0.0025/0.000125、opacity/scale/rotation 0.05/0.005/0.001。
- ADC / opacity reset: 有効 / 有効。開始500、終了15,000、interval 100、gradient threshold 0.0002、percent dense 0.01、opacity prune 0.005、reset interval 3,000、max screen radius 20.0。

## 4. 取得時点の学習経過

初回調査時点のsnapshotであり、Markdown作成中にもログは増加し得る。

| Iteration | Loss | PSNR [dB] | Gaussian数 | Elapsed [s] | VRAM [MiB] |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 10 | 0.419068 | 8.7134 | 20,000 | 160.0 | 不明 |
| 20 | 0.371283 | 9.4718 | 20,000 | 327.2 | 不明 |
| 50 | 0.246135 | 16.4615 | 20,000 | 815.0 | 不明 |

- 取得時点ではADC event、gradient statistics、screen radius diagnostics、opacity resetのログはまだない。

## 5. 最終結果

- Mean PSNR / SSIM / LPIPS: 未完了
- 最終Gaussian数 / 総学習時間 / Peak VRAM: 未完了
- NaN / Inf / CUDA OOM / Exception: 取得時点のJSONLに記録なし。最終判断は未完了。
- checkpoint / render / evaluation: 取得時点ではなし
- 出力ファイル: `config.yaml`、増加中の`train_log.jsonl`
- 結果・考察、前runとの比較、次の実験: 学習完了後に要追記
