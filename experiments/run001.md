# run001 実験記録

## 1. 概要

- Run: `output/run001`
- 実行日時: 不明
- 状態: 不明（config以外の通常ファイルなし）
- 実験目的: 要追記
- 比較対象: 要追記

## 2. 再現情報

- Git branch / commit / working tree / push状況: 不明
- 実行環境: device `auto`、dtype `float32`、seed `0`。OS、GPU、CUDA、PyTorch、Pythonは不明。
- データ: `camera_poses_blender.json`、resolution scale `1.0`、8枚ごとをtest、黒背景。画像数と実解像度は実行記録としては不明。

## 3. 学習条件

| 項目 | 設定値 |
| --- | --- |
| 初期Gaussian数 / SH degree / 初期opacity | 1,000 / 3 / 0.1 |
| Iterations / Optimizer | 30,000 / Adam |
| position LR | 0.00016 → 0.0000016 |
| SH DC / rest LR | 0.0025 / 0.000125 |
| opacity / scale / rotation LR | 0.05 / 0.005 / 0.001 |
| D-SSIM weight | 0.2 |
| ADC / opacity reset | 無効 / 無効 |

## 4. 実行・結果

- 実行コマンド: 不明
- 学習ログ、checkpoint、render、evaluation: なし
- Mean PSNR / 最終Gaussian数 / 総学習時間 / Peak VRAM: 不明
- 異常、変更理由、考察、次の実験: 要追記
- 情報源: `output/run001/config.yaml`
